SUBDESIGN frequencia_contador_1Hz
(
	clock,enable : INPUT;	
	clock_saida: OUTPUT;
)

VARIABLE
	count[25..0] : DFF;
	flag : NODE;
	
BEGIN
	count[].clk = clock;
	
	IF enable THEN	
		IF count[].q < 26214400 THEN
			count[].d = count[].q + 1;
			flag = GND;
		ELSIF count[].q >= 26214400 & count[].q < 52428800 THEN
			count[].d = count[].q + 1;
			flag = VCC;
		ELSE
			count[].d = 0;		
		END IF;
	ELSE
		count[].d = count[].q;
	END IF;
	
	clock_saida = flag;

END;